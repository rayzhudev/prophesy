{
  "name": "prophesy",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "web",
    "backend"
  ],
  "scripts": {
    "dev": "bun run --cwd backend dev & bun run --cwd web dev",
    "build": "bun run --cwd web build && bun run --cwd backend build"
  }
}
